---
// Grayscale carrier logos to build trust
// Shows the carriers we can shop for rates
---

<section class="py-8 bg-white border-y border-gray-100">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <p class="text-center text-sm text-gray-500 mb-6">We shop rates from 20+ top carriers including:</p>

    <div class="flex flex-wrap items-center justify-center gap-8 md:gap-12 opacity-60 grayscale">
      <!-- Progressive -->
      <div class="h-8 flex items-center" title="Progressive">
        <svg class="h-6 w-auto" viewBox="0 0 200 40" fill="currentColor">
          <text x="0" y="30" font-family="Arial, sans-serif" font-size="24" font-weight="bold" fill="#444">Progressive</text>
        </svg>
      </div>

      <!-- Travelers -->
      <div class="h-8 flex items-center" title="Travelers">
        <svg class="h-6 w-auto" viewBox="0 0 150 40" fill="currentColor">
          <text x="0" y="30" font-family="Arial, sans-serif" font-size="24" font-weight="bold" fill="#444">Travelers</text>
        </svg>
      </div>

      <!-- Nationwide -->
      <div class="h-8 flex items-center" title="Nationwide">
        <svg class="h-6 w-auto" viewBox="0 0 180 40" fill="currentColor">
          <text x="0" y="30" font-family="Arial, sans-serif" font-size="24" font-weight="bold" fill="#444">Nationwide</text>
        </svg>
      </div>

      <!-- Hartford -->
      <div class="h-8 flex items-center" title="The Hartford">
        <svg class="h-6 w-auto" viewBox="0 0 140 40" fill="currentColor">
          <text x="0" y="30" font-family="Arial, sans-serif" font-size="24" font-weight="bold" fill="#444">Hartford</text>
        </svg>
      </div>

      <!-- Safeco -->
      <div class="h-8 flex items-center" title="Safeco">
        <svg class="h-6 w-auto" viewBox="0 0 120 40" fill="currentColor">
          <text x="0" y="30" font-family="Arial, sans-serif" font-size="24" font-weight="bold" fill="#444">Safeco</text>
        </svg>
      </div>

      <!-- Mercury -->
      <div class="h-8 flex items-center hidden md:flex" title="Mercury">
        <svg class="h-6 w-auto" viewBox="0 0 130 40" fill="currentColor">
          <text x="0" y="30" font-family="Arial, sans-serif" font-size="24" font-weight="bold" fill="#444">Mercury</text>
        </svg>
      </div>

      <!-- And More -->
      <div class="h-8 flex items-center text-gray-500 font-medium">
        +15 more
      </div>
    </div>
  </div>
</section>
